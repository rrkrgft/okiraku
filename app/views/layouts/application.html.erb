<!DOCTYPE html>
<html>
  <head>
    <title>Okiraku</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://code.highcharts.com/highcharts.js"></script>
  </head>

  <body>
    <div style="min-height: 100vh; position: relative; padding-bottom: 60px; box-sizing: border-box;">
      <head>
        <nav class="app-title navbar navbar-expand-md navbar-dark bg-primary">
          <div class="navbar-brand" style="margin-left: 20px;"><%= link_to "O•KI•RAKU", top_index_path, class: 'nav-link' %></div>
          <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to "投稿の一覧", posts_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "お気に入り一覧", favorites_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "ラベル", labels_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "自分の投稿", users_path, class: "nav-link" %></li>
              <% if current_user.admin == true %>
                <li class="nav-item"><%= link_to "管理者画面", rails_admin_path, class: "nav-link" %></li>
              <% end %>
              <li class="nav-item"><%= link_to "ログアウト", destroy_user_session_path,method: :delete, class: "nav-link" %></li>
            <% else %>
              <li class="nav-item"><%= link_to "ログイン", new_user_session_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "新規登録", new_user_registration_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </nav>
      </head>

      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <div class='container'>
        <%= yield %>
      </div>
    </body>
    <footer class="footer" style="position: absolute; bottom: 0; width: 100%;">
      <nav class="app-title navbar navbar-expand-md navbar-dark bg-primary">
        <div class="container">
          <p class="text-muted navbar-nav"><%= link_to "O・KI・RAKUについて", top_index_path, class: "nav-link" %></p>
        </div>
      </nav>
    </footer>
  </div>
</html>
